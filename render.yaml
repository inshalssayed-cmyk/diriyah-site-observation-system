services:
  - type: web
    name: diriyah-shared-observation-system
    runtime: node
    plan: free
    buildCommand: npm install && npm run check
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: diriyah-observation-db
          property: connectionString
      - key: STATUS_PASSWORD
        value: "1353"
      - key: MSO_STATUS_PASSWORD
        value: "1353"
      - key: ONE_HOTEL_STATUS_PASSWORD
        value: "9528"
      - key: GEMINI_MODEL
        value: gemini-3.1-flash-lite
      - key: GEMINI_API_KEY
        sync: false
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false

databases:
  - name: diriyah-observation-db
    plan: free
